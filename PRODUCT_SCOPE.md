# PRODUCT_SCOPE.md — VDI Taxi Pilot

> Generated: 2026-02-13 | Source: полный аудит исходного кода

---

## 1. ТЕКУЩЕЕ СОСТОЯНИЕ КОДА

### 1.1 Экраны и компоненты

| # | Экран | Файл | Описание |
|---|-------|------|----------|
| 1 | **Login** | `src/components/LoginScreen.tsx` | Логин + пароль + 2FA (OTP 6 цифр) |
| 2 | **Onboarding** | `src/components/OnboardingScreen.tsx` | 3-шаговый визард (Telegram, PWA, видео-тур) |
| 3 | **Dashboard** | `src/components/DashboardScreen.tsx` | Главный экран: избранные, шаблоны, все сервисы, статистика |
| 4 | **Session** | `src/components/SessionScreen.tsx` | Активная VDI-сессия (placeholder удалённого рабочего стола) |
| 5 | **SessionEnd** | `src/components/SessionEndScreen.tsx` | Итог завершённой сессии + Telegram-уведомление |
| 6 | **Admin** | `src/components/AdminScreen.tsx` | Админ-панель с 5 вкладками |
| 7 | **Profile** | `src/components/ProfileScreen.tsx` | Профиль: контакты, избранные, обучение |

**Вспомогательные компоненты:**

| Файл | Назначение |
|------|-----------|
| `src/components/admin/HealthTab.tsx` | Вкладка "Здоровье": VM-карточки, VPN, статус сервисов |
| `src/components/admin/ServicesTab.tsx` | Вкладка "Сервисы": таблица подписок, CRUD модалки |
| `src/components/NavLink.tsx` | Обёртка над `react-router-dom` NavLink (не используется в текущем коде) |
| `src/components/ui/*` | ~50 shadcn/ui компонентов (Button, Dialog, Table, Tabs, Calendar, etc.) |

**Навигация:**

| Файл | Роль |
|------|------|
| `src/App.tsx` | Единственный route `/` → `Index`, fallback `*` → `NotFound` |
| `src/pages/Index.tsx` | State-machine роутер: `useState<Screen>` переключает 7 экранов |
| `src/pages/NotFound.tsx` | 404-страница |

### 1.2 Кликабельные UI-элементы и их действия

#### LoginScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Input "Логин" | Ввод текста | Сохраняется в local state, **нигде не проверяется** |
| Input "Пароль" | Ввод текста | Сохраняется в local state, **нигде не проверяется** |
| InputOTP "Код 2FA" | Ввод 6 цифр | Сохраняется в local state, **нигде не проверяется** |
| Button "Войти" | `onLogin()` | Переход на onboarding (если `firstLogin`) или dashboard. **Нет валидации, нет API** |

#### OnboardingScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Input "Telegram username" | Ввод текста | Local state, **никуда не отправляется** |
| Button "Далее" (шаг 1→2) | Переход на шаг 2 | Просто `setStep(1)` |
| Button "Далее" (шаг 2→3) | Переход на шаг 3 | Просто `setStep(2)` |
| Button "Начать работу" | `onComplete()` | `setFirstLogin(false)` + переход на Dashboard |
| Placeholder "Инструкция (скриншот)" | — | Серый прямоугольник, нет изображения |
| Placeholder "Видео-тур (2 мин)" | — | Серый прямоугольник, нет видео |

#### DashboardScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Карточка избранного (свободен) | `onBook(slot.name)` | Переход на SessionScreen |
| Карточка избранного (занят) | `handleQueue()` | Toast "Вы в очереди", **нет реальной очереди** |
| "Редактировать" (избранные) | `onProfile()` | Переход на ProfileScreen |
| Button "Запустить" (шаблон) | `handleTemplateLaunch()` | Toast + `setTimeout` 600ms → `onBook()`. **Нет реальной мульти-слот логики** |
| "★" на карточке | `toggleFavorite()` | Локальный toggle в `Set<string>`. **Не персистится** |
| "📅" на карточке | Открывает модалку бронирования | Модалка с Calendar + Select + RadioGroup |
| "?" на карточке | Открывает tutorial модалку | Модалка с placeholder-видео |
| Button "Занять" | `onBook(slot.name)` | Переход на SessionScreen. **Нет проверки доступности, нет API** |
| Button "В очередь" | `handleQueue()` | Toast-уведомление. **Нет реальной очереди** |
| Button "Забронировать" (модалка) | `handleBookingConfirm()` | Toast "Бронь подтверждена". **Нет API, бронь не сохраняется** |
| "+ Создать шаблон" | — | **Мёртвая кнопка**, ничего не делает (нет `onClick`) |
| Burger-меню (mobile) | Toggle mobile menu | Показывает/скрывает мобильное меню |
| Профиль аватар "А" | `onProfile()` | Переход на ProfileScreen |
| Settings иконка | `onAdmin()` | Переход на AdminScreen |
| LogOut иконка | `onLogout()` | Переход на LoginScreen. **Нет сброса токена/сессии** |

#### SessionScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Button "Освободить" | Открывает AlertDialog | Подтверждение завершения |
| "Да, завершить" | `onEnd()` | Переход на SessionEndScreen. **Нет API-вызова** |
| "Отмена" | Закрывает dialog | — |
| Таймер "00:14:32" | — | **Захардкожен**, не считает реальное время |
| "Удалённый рабочий стол" | — | Placeholder (dashed border). **Нет Guacamole iframe** |

#### SessionEndScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Button "Вернуться на дашборд" | `onBack()` | Переход на Dashboard |
| Данные "13:42–14:18 (36 мин)" | — | **Захардкожены** |
| "4 вкладки, 1 файл" | — | **Захардкожены** |
| Telegram-сообщение | — | **Захардкожено**, не отправляется реально |

#### AdminScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Button "Назад к дашборду" | `onBack()` | Переход на Dashboard |
| Tab "Загрузка" | Показывает bar chart | **Захардкоженные проценты** |
| Tab "Сервисы" | Показывает ServicesTab | CRUD таблица, **только в local state** |
| Tab "Пользователи" | Показывает таблицу | **Захардкоженный список** |
| Tab "Шаблоны" | Показывает таблицу | **Захардкоженный список** |
| Tab "Здоровье" | Показывает HealthTab | **Захардкоженные VM/сервисы** |
| "+ Создать шаблон" (Tab "Шаблоны") | Открывает модалку | Модалка с формой, Toast при создании. **Не добавляется в таблицу** |

#### AdminScreen > ServicesTab

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| "+ Добавить сервис" | Открывает модалку | Добавляет в local state массив |
| Pencil иконка | Открывает модалку редактирования | Редактирует в local state |
| Switch (активен/выключен) | `toggleActive()` | Переключает в local state |
| Button "Сохранить" (модалка) | `handleSave()` / `handleAdd()` | Toast + обновление local state. **Нет API** |

#### AdminScreen > HealthTab

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Button "Перезагрузить" (VM) | Toast "Перезагрузка" | **Нет реального действия** |
| VM-карточки | Read-only | **Захардкоженные данные** |
| VPN-статус | Read-only | **Захардкожен: Connected, IP 185.156.xx.xx** |
| Статус сервисов | Read-only | **Захардкожены** |

#### ProfileScreen

| Элемент | Действие | Реальная логика |
|---------|----------|-----------------|
| Button "Назад к дашборду" | `onBack()` | Переход на Dashboard |
| Input "Telegram username" | Ввод текста | `defaultValue="@anna"`, **не сохраняется** |
| Button "Сохранить" (контакты) | Toast "Сохранено" | **Нет API** |
| Checkbox'ы избранных | `toggle(id)` | Local state. **Не синхронизирован с DashboardScreen** |
| Button "Сохранить" (избранные) | Toast "Сохранено" | **Нет API, не синхронизирован с Dashboard** |
| Видео-карточки (5 штук) | Открывают модалку | Модалка с placeholder-видео |

### 1.3 Захардкоженные данные (моки)

| Данные | Файл : строки | Содержимое |
|--------|---------------|------------|
| **Список сервисов (slots)** | `DashboardScreen.tsx:34-75` | 10 слотов в 5 категориях, `available`/`occupant`/`minutes` |
| **Шаблоны задач** | `DashboardScreen.tsx:86-90` | 3 шаблона (Ресерч, Видео, Презентация) |
| **Избранные по умолчанию** | `DashboardScreen.tsx:92` | `Set(["ppx-1", "nb-drive"])` |
| **Временные слоты** | `DashboardScreen.tsx:94-98` | 09:00–18:00 с шагом 30 мин |
| **Статистика загрузки** | `DashboardScreen.tsx:311-315` | "PPX 62% / Gemini 41% / GPT 28% / HF 11%" |
| **Имя пользователя** | `DashboardScreen.tsx:166` | "Привет, Анна" / аватар "А" |
| **Дата бронирования** | `DashboardScreen.tsx:119` | `new Date(2026, 1, 14)` — 14 фев 2026 |
| **Загрузка сервисов (admin)** | `AdminScreen.tsx:21-32` | 10 записей с процентами |
| **Список пользователей** | `AdminScreen.tsx:34-43` | 8 пользователей (Анна, Иван, Олег...) |
| **Шаблоны (admin)** | `AdminScreen.tsx:45-49` | 3 шаблона с usage-счётчиками |
| **Подписки/сервисы** | `admin/ServicesTab.tsx:25-36` | 10 сервисов с логинами/паролями/стоимостью |
| **VM-карточки** | `admin/HealthTab.tsx:6-11` | 5 VM (online/offline, uptime, occupant) |
| **Статусы сервисов** | `admin/HealthTab.tsx:13-24` | 10 сервисов (ok/warn/error) |
| **VPN-статус** | `admin/HealthTab.tsx:71-79` | "Connected", IP 185.156.xx.xx |
| **Список сервисов (profile)** | `ProfileScreen.tsx:8-18` | 9 сервисов (без Lovable Team) |
| **Видео-обучение** | `ProfileScreen.tsx:22-28` | 5 видео с заголовками и длительностью |
| **Данные сессии** | `SessionEndScreen.tsx:19-21` | "13:42–14:18 (36 мин)", 4 вкладки, 1 файл |
| **Таймер сессии** | `SessionScreen.tsx:30` | "00:14:32" (статичный) |
| **Telegram-сообщение** | `SessionEndScreen.tsx:29-34` | Захардкоженный текст уведомления |

### 1.4 npm-зависимости

#### Runtime

| Пакет | Версия | Используется? |
|-------|--------|---------------|
| `react` / `react-dom` | ^18.3.1 | Да, core |
| `react-router-dom` | ^6.30.1 | Да, `App.tsx` (Routes), `NavLink.tsx` |
| `@tanstack/react-query` | ^5.83.0 | Инициализирован `QueryClientProvider` в `App.tsx`, **фактически не используется** (нет `useQuery`/`useMutation`) |
| `react-hook-form` | ^7.61.1 | **Не используется** (формы на голом `useState`) |
| `@hookform/resolvers` | ^3.10.0 | **Не используется** |
| `zod` | ^3.25.76 | **Не используется** |
| `date-fns` | ^3.6.0 | Да, `DashboardScreen.tsx` (`format`) |
| `lucide-react` | ^0.462.0 | Да, иконки по всему проекту |
| `sonner` | ^1.7.4 | Да, Sonner toaster в `App.tsx` |
| `recharts` | ^2.15.4 | **Не используется** (статистика — простые div-бары) |
| `next-themes` | ^0.3.0 | **Не используется** |
| `cmdk` | ^1.1.1 | **Не используется** (Command компонент не задействован) |
| `embla-carousel-react` | ^8.6.0 | **Не используется** |
| `react-day-picker` | ^8.10.1 | Да, через shadcn Calendar |
| `react-resizable-panels` | ^2.1.9 | **Не используется** |
| `vaul` | ^0.9.9 | **Не используется** (Drawer не задействован) |
| `input-otp` | ^1.4.2 | Да, `LoginScreen.tsx` (InputOTP) |
| `class-variance-authority` | ^0.7.1 | Да, shadcn internals |
| `clsx` | ^2.1.1 | Да, `lib/utils.ts` |
| `tailwind-merge` | ^2.6.0 | Да, `lib/utils.ts` |
| `tailwindcss-animate` | ^1.0.7 | Да, анимации |
| `@radix-ui/*` (20 пакетов) | Разные | Используются через shadcn/ui компоненты |

#### Dev

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `vite` | ^5.4.19 | Build tool |
| `@vitejs/plugin-react-swc` | ^3.11.0 | React SWC compiler |
| `typescript` | ^5.8.3 | TypeScript |
| `tailwindcss` | ^3.4.17 | Стили |
| `postcss` / `autoprefixer` | — | PostCSS pipeline |
| `eslint` + plugins | ^9.32.0 | Линтинг |
| `vitest` | ^3.2.4 | Тесты (единственный тест — пустой placeholder) |
| `@testing-library/react` + `jest-dom` | — | Testing utils |
| `jsdom` | ^20.0.3 | DOM environment для тестов |
| `lovable-tagger` | ^1.1.13 | Dev-плагин Lovable (component tagger) |
| `@tailwindcss/typography` | ^0.5.16 | **Не используется** |

---

## 2. АРХИТЕКТУРА ФРОНТЕНДА

### 2.1 Дерево компонентов

```
App.tsx
├── QueryClientProvider (react-query, не используется)
├── TooltipProvider
├── Toaster (radix toast)
├── Sonner (sonner toast)
└── BrowserRouter
    └── Routes
        ├── "/" → Index.tsx (state-machine роутер)
        │   ├── screen="login"      → LoginScreen
        │   ├── screen="onboarding" → OnboardingScreen
        │   ├── screen="dashboard"  → DashboardScreen
        │   │   ├── Favorites section (slot cards)
        │   │   ├── Templates section (template cards)
        │   │   ├── All Services section (category → slot cards)
        │   │   ├── Stats bar
        │   │   ├── Booking Dialog (Calendar, Select, RadioGroup)
        │   │   └── Tutorial Dialog (placeholder video)
        │   ├── screen="session"    → SessionScreen
        │   │   └── AlertDialog (confirm end)
        │   ├── screen="sessionEnd" → SessionEndScreen
        │   ├── screen="admin"      → AdminScreen
        │   │   ├── Tab "Загрузка" (bar chart)
        │   │   ├── Tab "Сервисы"  → ServicesTab
        │   │   │   └── Dialog (add/edit service form)
        │   │   ├── Tab "Пользователи" (table)
        │   │   ├── Tab "Шаблоны" (table + create dialog)
        │   │   └── Tab "Здоровье" → HealthTab
        │   └── screen="profile"    → ProfileScreen
        │       └── Dialog (video player placeholder)
        └── "*" → NotFound
```

### 2.2 Управление состоянием

**Глобальное состояние: ОТСУТСТВУЕТ**

Всё приложение построено на `useState` внутри компонентов. Нет Redux, Zustand, Context API или какого-либо глобального store.

| Состояние | Где живёт | Проблема |
|-----------|-----------|----------|
| `screen` (текущий экран) | `Index.tsx:13` | Навигация через `useState`, не через URL |
| `activeSlot` (текущий слот) | `Index.tsx:14` | Передаётся как prop |
| `firstLogin` | `Index.tsx:15` | При F5 сбрасывается, нет persist |
| `favorites` (избранные) | `DashboardScreen.tsx:114` | **Изолирован** от ProfileScreen |
| `checked` (избранные в профиле) | `ProfileScreen.tsx:36` | **Изолирован** от DashboardScreen |
| `services` (список подписок) | `ServicesTab.tsx:42` | Local state, пропадает при уходе |
| Login/password/OTP | `LoginScreen.tsx:12-14` | Не отправляются никуда |
| Telegram username | `OnboardingScreen.tsx:13` | Не сохраняется |
| Booking date/time/duration | `DashboardScreen.tsx:119-121` | Не отправляется |

**Критическая проблема**: `favorites` в DashboardScreen и `checked` в ProfileScreen — два независимых `Set<string>`, которые не синхронизированы. Изменения в одном экране не видны в другом.

### 2.3 Навигация между экранами

Навигация реализована как **state machine** в `src/pages/Index.tsx`, а не через URL-роуты.

```
login ──(войти)──→ onboarding ──(завершить)──→ dashboard
  ↑                                              │
  └─────────(выйти)──────────────────────────────┤
                                                  ├──(занять/запустить)──→ session ──(освободить)──→ sessionEnd ──(назад)──→ dashboard
                                                  ├──(настройки)──→ admin ──(назад)──→ dashboard
                                                  └──(профиль)──→ profile ──(назад)──→ dashboard
```

**Последствия:**
- Нет deep-linking (нельзя дать ссылку на конкретный экран)
- Кнопка "Назад" браузера не работает
- F5 всегда возвращает на login
- История навигации не сохраняется

### 2.4 Точки подключения реального API

| Действие | Файл : функция | Что нужно от API |
|----------|-----------------|------------------|
| **Аутентификация** | `LoginScreen.tsx` → `onLogin()` | `POST /auth/login` (login, password, otp) → token |
| **Привязка Telegram** | `OnboardingScreen.tsx` → шаг 1 | `POST /users/me/telegram` (username) |
| **Получить список сервисов** | `DashboardScreen.tsx` → `categories` | `GET /slots` → real-time availability |
| **Занять слот** | `DashboardScreen.tsx` → `onBook()` | `POST /sessions` (slotId) → session + Guacamole URL |
| **Встать в очередь** | `DashboardScreen.tsx` → `handleQueue()` | `POST /queue` (slotId) |
| **Забронировать** | `DashboardScreen.tsx` → `handleBookingConfirm()` | `POST /bookings` (slotId, date, time, duration) |
| **Toggle избранное** | `DashboardScreen.tsx` → `toggleFavorite()` | `POST /users/me/favorites` (slotId) |
| **Завершить сессию** | `SessionScreen.tsx` → `onEnd()` | `POST /sessions/:id/end` → артефакты |
| **Получить итоги сессии** | `SessionEndScreen.tsx` | `GET /sessions/:id/summary` |
| **Загрузка сервисов (admin)** | `AdminScreen.tsx` → loadData | `GET /admin/load-stats` |
| **Список пользователей** | `AdminScreen.tsx` → users | `GET /admin/users` |
| **CRUD шаблонов** | `AdminScreen.tsx` → templates | `GET/POST/PUT/DELETE /admin/templates` |
| **CRUD подписок** | `ServicesTab.tsx` | `GET/POST/PUT /admin/services` |
| **Здоровье VM** | `HealthTab.tsx` → vms | `GET /admin/health/vms` |
| **Статус сервисов** | `HealthTab.tsx` → services | `GET /admin/health/services` |
| **Перезагрузка VM** | `HealthTab.tsx` → button | `POST /admin/vms/:name/reboot` |
| **VPN-статус** | `HealthTab.tsx` → VPN | `GET /admin/health/vpn` |
| **Профиль: сохранить TG** | `ProfileScreen.tsx` → "Сохранить" | `PUT /users/me` (telegram) |
| **Профиль: сохранить избранные** | `ProfileScreen.tsx` → "Сохранить" | `PUT /users/me/favorites` (ids[]) |
| **Guacamole iframe** | `SessionScreen.tsx` → placeholder | WebSocket / iframe URL от `POST /sessions` |

---

## 3. ЧТО НЕ РЕАЛИЗОВАНО

### 3.1 Мёртвые кнопки

| Элемент | Файл : строка | Проблема |
|---------|---------------|----------|
| **"+ Создать шаблон"** (Dashboard, зона шаблонов) | `DashboardScreen.tsx:237` | Нет `onClick` handler. Кнопка ничего не делает |

### 3.2 Формы без валидации

| Форма | Файл | Что не проверяется |
|-------|------|--------------------|
| **Login** | `LoginScreen.tsx` | Пустой логин/пароль/OTP — кнопка "Войти" работает. Нет проверки формата email, длины пароля, полноты OTP |
| **Onboarding: Telegram** | `OnboardingScreen.tsx` | Пустой username — "Далее" работает. Нет проверки формата `@username` |
| **Бронирование** | `DashboardScreen.tsx` | Можно забронировать прошедшую дату. Нет проверки конфликтов |
| **Создание шаблона (admin)** | `AdminScreen.tsx` | Есть `disabled={!tplName}`, но нет проверки уникальности имени. Можно создать шаблон без слотов |
| **Редактирование/добавление сервиса** | `admin/ServicesTab.tsx` | Есть `disabled={!form.service}`, но URL, логин, пароль не валидируются |
| **Профиль: Telegram** | `ProfileScreen.tsx` | Нет проверки формата. "Сохранить" показывает toast без реального сохранения |
| **Профиль: избранные** | `ProfileScreen.tsx` | Можно снять все чекбоксы. "Сохранить" — toast без сохранения |

### 3.3 Отсутствующие функции

| Функция | Описание |
|---------|----------|
| **Реальная аутентификация** | Нет API, нет токенов, нет сессий |
| **Guacamole iframe** | `SessionScreen.tsx` показывает placeholder вместо удалённого рабочего стола |
| **Реальный таймер сессии** | `00:14:32` захардкожен, не считает время |
| **Очередь на слот** | Toast-уведомление, но нет backend-очереди и нет WebSocket для уведомлений |
| **Бронирование** | Форма работает, но бронь никуда не сохраняется |
| **Telegram-уведомления** | Текст показывается, но реально не отправляется |
| **Артефакты сессии** | "4 вкладки, 1 файл" — мок, нет сбора артефактов |
| **Синхронизация избранных** | Dashboard и Profile хранят свои копии |
| **Persist состояния** | F5 = потеря всего (login, favorites, session) |
| **Real-time статус слотов** | Статусы "Свободен/Занят" — моки |
| **Роли пользователей** | Нет разделения admin/user. Кнопка "Админ-панель" видна всем |
| **PWA offline** | `manifest.json` есть, но нет service worker |
| **Тесты** | Единственный тест — `expect(true).toBe(true)` |

---

## 4. РЕКОМЕНДАЦИИ ПО БЭКЕНДУ

### 4.1 API-эндпоинты

```
# Auth
POST   /api/auth/login              { login, password, otp } → { token, user, isFirstLogin }
POST   /api/auth/logout             {} → 204
GET    /api/auth/me                 → { user }

# Users
GET    /api/users/me                → { id, name, telegram, role, favorites[] }
PUT    /api/users/me                { telegram } → { user }
PUT    /api/users/me/favorites      { slotIds[] } → { favorites[] }
POST   /api/users/me/onboarding     { telegram } → 204

# Slots (сервисы/слоты)
GET    /api/slots                   → { categories[{ title, accent, slots[] }] }
                                      Каждый slot: { id, name, available, occupant?, minutes?, category }

# Sessions
POST   /api/sessions               { slotId } → { sessionId, guacamoleUrl, startedAt }
POST   /api/sessions/:id/end       → { summary: { duration, tabs, files, telegramSent } }
GET    /api/sessions/:id/summary    → { slotName, startTime, endTime, duration, tabs, files }

# Bookings
POST   /api/bookings               { slotId, date, startTime, duration } → { bookingId }
DELETE /api/bookings/:id            → 204
GET    /api/bookings/my             → { bookings[] }

# Queue
POST   /api/queue                   { slotId } → { position }
DELETE /api/queue/:slotId           → 204

# Templates
GET    /api/templates               → { templates[] }
POST   /api/templates               { name, icon, slotIds[], url? } → { template }
PUT    /api/templates/:id           { name, icon, slotIds[], url? } → { template }
DELETE /api/templates/:id           → 204
POST   /api/templates/:id/launch    → { sessionId, guacamoleUrl }

# Admin: Services (подписки)
GET    /api/admin/services          → { services[{ id, name, plan, cost, renewal, status, active, url, login, password, category }] }
POST   /api/admin/services          { ... } → { service }
PUT    /api/admin/services/:id      { ... } → { service }

# Admin: Users
GET    /api/admin/users             → { users[{ name, telegram, sessions, hours }] }

# Admin: Stats
GET    /api/admin/load-stats        → { loads[{ slotId, percentage }] }
GET    /api/admin/queue-stats       → { weeklyQueues, avgWaitMinutes }

# Admin: Health
GET    /api/admin/health/vms        → { vms[{ name, online, occupant, uptime, needsReboot }] }
POST   /api/admin/vms/:name/reboot  → 202
GET    /api/admin/health/vpn        → { connected, ip }
GET    /api/admin/health/services   → { services[{ name, status: "ok"|"warn"|"error" }] }

# WebSocket
WS     /api/ws/slots                → real-time slot availability updates
WS     /api/ws/queue                → queue position updates + "slot freed" notifications
```

### 4.2 Модель данных

```
┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
│    users      │     │    slots         │     │    categories    │
├──────────────┤     ├──────────────────┤     ├──────────────────┤
│ id           │     │ id               │     │ id               │
│ login        │     │ name             │     │ title            │
│ password_hash│     │ category_id  ────┼────→│ accent_color     │
│ otp_secret   │     │ service_id   ────┼──┐  └──────────────────┘
│ telegram     │     │ available        │  │
│ role (enum)  │     │ current_session  │  │  ┌──────────────────┐
│ is_first_login│    └──────────────────┘  └─→│    services      │
│ created_at   │                              ├──────────────────┤
└──────┬───────┘     ┌──────────────────┐     │ id               │
       │             │    sessions      │     │ name             │
       │             ├──────────────────┤     │ plan             │
       │             │ id               │     │ cost             │
       │             │ user_id      ────┼──→  │ renewal_date     │
       │             │ slot_id          │     │ url              │
       │             │ started_at       │     │ login            │
       │             │ ended_at         │     │ password (encr)  │
       │             │ guacamole_conn_id│     │ category_id      │
       │             │ tabs_saved       │     │ active           │
       │             │ files_saved      │     └──────────────────┘
       │             └──────────────────┘
       │
       │             ┌──────────────────┐     ┌──────────────────┐
       │             │    bookings      │     │    queue         │
       │             ├──────────────────┤     ├──────────────────┤
       ├────────────→│ id               │     │ id               │
       │             │ user_id          │     │ user_id      ────┼──→
       │             │ slot_id          │     │ slot_id          │
       │             │ date             │     │ position         │
       │             │ start_time       │     │ created_at       │
       │             │ duration_min     │     └──────────────────┘
       │             │ status (enum)    │
       │             └──────────────────┘
       │
       │             ┌──────────────────┐     ┌──────────────────────┐
       │             │   user_favorites │     │    templates         │
       │             ├──────────────────┤     ├──────────────────────┤
       └────────────→│ user_id          │     │ id                   │
                     │ slot_id          │     │ name                 │
                     └──────────────────┘     │ icon                 │
                                              │ creator_id           │
                     ┌──────────────────┐     │ url                  │
                     │  template_slots  │     │ usage_count          │
                     ├──────────────────┤     └──────────────────────┘
                     │ template_id  ────┼──→
                     │ slot_id          │
                     └──────────────────┘

┌──────────────────┐
│   virtual_machines│
├──────────────────┤
│ name             │
│ online           │
│ current_session  │
│ uptime_since     │
│ ip_address       │
└──────────────────┘
```

**Enums:**
- `user.role`: `admin`, `user`
- `booking.status`: `pending`, `confirmed`, `cancelled`, `expired`
- `health_service.status`: `ok`, `warn`, `error`

### 4.3 Интеграции

#### Apache Guacamole (VDI)

Основная интеграция — предоставление удалённого рабочего стола через браузер.

| Что | Как |
|-----|-----|
| **Протокол** | guacamole-common-js → WebSocket → guacd |
| **Точка встраивания** | `SessionScreen.tsx:37-41` — заменить placeholder на `<iframe>` или Guacamole.Client canvas |
| **Поток** | `POST /sessions` → backend создаёт connection в Guacamole → возвращает `guacamoleUrl` с auth-token → фронт загружает iframe |
| **Завершение** | `POST /sessions/:id/end` → backend закрывает Guacamole connection + собирает артефакты |

#### Telegram Bot

| Что | Как |
|-----|-----|
| **Уведомления** | Bot отправляет сообщения пользователю при: завершении сессии, освобождении слота из очереди, напоминании о брони |
| **Привязка** | Onboarding шаг 1: пользователь вводит `@username` → backend отправляет confirmation через бота |
| **Артефакты** | После `POST /sessions/:id/end` backend формирует summary и отправляет через Telegram Bot API |
| **Точки в UI** | `OnboardingScreen.tsx` (привязка), `SessionEndScreen.tsx:29-34` (показ отправленного), `DashboardScreen.tsx:136` (уведомление об очереди) |

#### VPN

| Что | Как |
|-----|-----|
| **Мониторинг** | Backend проверяет VPN-статус (connected/disconnected, IP) |
| **Точка в UI** | `admin/HealthTab.tsx:67-80` |

---

## 5. ТЕХНИЧЕСКИЙ ДОЛГ

### 5.1 Архитектурный

| Проблема | Файлы | Влияние | Рекомендация |
|----------|-------|---------|--------------|
| **Навигация через useState вместо URL** | `pages/Index.tsx` | Нет deep-link, кнопка "назад" браузера не работает, F5 → login | Перевести на `react-router-dom` routes (`/dashboard`, `/session/:id`, `/admin`, `/profile`) |
| **Нет глобального state management** | Весь проект | Favorites не синхронизированы между Dashboard и Profile, user data дублируется | Добавить Context или Zustand для `user`, `favorites`, `session` |
| **Дублирование списка сервисов** | `DashboardScreen.tsx:34-75`, `AdminScreen.tsx:51-55`, `ServicesTab.tsx:25-36`, `ProfileScreen.tsx:8-18`, `HealthTab.tsx:13-24` | 5 копий одних данных с разными полями, расходятся | Единый source of truth (API или shared constant) |
| **Нет auth-guard** | `pages/Index.tsx` | Любой может перейти на любой экран через state | Добавить ProtectedRoute + redirect на login |
| **Нет ролевой модели** | `DashboardScreen.tsx:170` | Кнопка "Админ-панель" видна всем | Скрывать по `user.role` |

### 5.2 Неиспользуемый код и зависимости

| Что | Файл | Действие |
|-----|------|----------|
| `react-hook-form` + `@hookform/resolvers` + `zod` | `package.json` | Удалить или начать использовать для валидации форм |
| `recharts` | `package.json` | Удалить или заменить div-бары в AdminScreen на реальные графики |
| `next-themes` | `package.json` | Удалить (нет theme switching) |
| `cmdk` | `package.json` | Удалить (Command palette не используется) |
| `embla-carousel-react` | `package.json` | Удалить (Carousel не используется) |
| `react-resizable-panels` | `package.json` | Удалить (Resizable не используется) |
| `vaul` | `package.json` | Удалить (Drawer не используется) |
| `@tailwindcss/typography` | `package.json` (dev) | Удалить (prose-классы не используются) |
| `NavLink.tsx` | `src/components/NavLink.tsx` | Не используется ни одним компонентом — удалить или задействовать при переходе на URL-навигацию |
| `App.css` | `src/App.css` | Стили от Vite boilerplate (`.logo`, `.card`, `.read-the-docs`), не используются — удалить |
| ~30 shadcn/ui компонентов | `src/components/ui/` | `breadcrumb`, `carousel`, `chart`, `collapsible`, `command`, `context-menu`, `drawer`, `hover-card`, `menubar`, `navigation-menu`, `pagination`, `resizable`, `scroll-area`, `sheet`, `sidebar`, `skeleton`, `slider`, `toggle`, `toggle-group` — не используются |
| `lovable-tagger` | `package.json` (dev) | Dev-зависимость от Lovable, можно убрать если проект выходит из Lovable |

### 5.3 Безопасность

| Проблема | Файл | Серьёзность |
|----------|------|-------------|
| **Пароли сервисов в открытом виде** | `admin/ServicesTab.tsx:25-36` | КРИТИЧЕСКАЯ. `password: "pass123"` и т.д. захардкожены в исходном коде. В production должны храниться зашифрованно на backend |
| **Нет CSRF/XSS защиты** | — | Средняя. При подключении API нужно добавить |
| **OG-теги с Lovable branding** | `index.html:14-21` | Низкая. `og:title` = "Lovable App", `og:image` ведёт на lovable.dev |
| **Нет rate-limiting на login** | — | Средняя. При подключении API нужно добавить |

### 5.4 UX / QoL

| Проблема | Файл | Рекомендация |
|----------|------|--------------|
| **Нет loading states** | Весь проект | Добавить Skeleton/Spinner при загрузке данных |
| **Нет error states** | Весь проект | Добавить обработку ошибок API |
| **Нет empty states** | — | Добавить для пустых таблиц, отсутствия данных |
| **Таймер сессии не работает** | `SessionScreen.tsx:30` | Заменить `00:14:32` на реальный `useEffect` + `setInterval` |
| **Видео-placeholder'ы** | `OnboardingScreen.tsx`, `ProfileScreen.tsx`, `DashboardScreen.tsx` | 8 placeholder'ов для видео — нужны реальные видео или убрать |
| **PWA неполная** | `manifest.json`, нет SW | Добавить Service Worker для offline + push-уведомлений |
| **Нет `<title>` для экранов** | — | Добавить `document.title` при смене экрана |
| **Mobile: кнопка "Освободить" мелкая** | `SessionScreen.tsx:31` | Уже есть `md:h-9 h-12`, но стоит проверить на реальных устройствах |

### 5.5 Тесты

| Проблема | Файл |
|----------|------|
| Единственный тест — `expect(true).toBe(true)` | `src/test/example.test.ts` |
| Нет тестов для компонентов | — |
| Нет тестов для навигации | — |
| `@testing-library/react` установлен, но не используется | `package.json` |

---

## Приложение: Файловая карта

```
vdi-taxi-pilot/
├── public/
│   ├── favicon.ico
│   ├── manifest.json          # PWA manifest (неполный, нет SW)
│   ├── placeholder.svg
│   └── robots.txt
├── src/
│   ├── components/
│   │   ├── admin/
│   │   │   ├── HealthTab.tsx      # VM, VPN, статус сервисов (моки)
│   │   │   └── ServicesTab.tsx    # CRUD подписок (local state)
│   │   ├── ui/                    # ~50 shadcn/ui компонентов
│   │   ├── AdminScreen.tsx        # 5 вкладок (моки)
│   │   ├── DashboardScreen.tsx    # Главный экран (моки)
│   │   ├── LoginScreen.tsx        # Логин без валидации
│   │   ├── NavLink.tsx            # НЕ ИСПОЛЬЗУЕТСЯ
│   │   ├── OnboardingScreen.tsx   # 3-step визард (моки)
│   │   ├── ProfileScreen.tsx      # Профиль (моки)
│   │   ├── SessionEndScreen.tsx   # Итоги сессии (моки)
│   │   └── SessionScreen.tsx      # VDI placeholder
│   ├── hooks/
│   │   ├── use-mobile.tsx         # Media query hook (768px)
│   │   └── use-toast.ts           # Toast state management
│   ├── lib/
│   │   └── utils.ts               # cn() helper
│   ├── pages/
│   │   ├── Index.tsx              # State-machine роутер
│   │   └── NotFound.tsx           # 404
│   ├── test/
│   │   ├── example.test.ts        # Пустой тест
│   │   └── setup.ts               # Jest-DOM setup
│   ├── App.css                    # НЕ ИСПОЛЬЗУЕТСЯ (Vite boilerplate)
│   ├── App.tsx                    # Root: providers + router
│   ├── index.css                  # Tailwind + dark theme + custom animation
│   ├── main.tsx                   # Entry point
│   └── vite-env.d.ts             # Vite types
├── .lovable/
│   └── plan.md                    # Lovable dev plan
├── components.json                # shadcn/ui config
├── eslint.config.js
├── index.html                     # Lovable OG-теги (нужно обновить)
├── package.json
├── postcss.config.js
├── tailwind.config.ts
├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
├── vite.config.ts                 # Port 8080, lovable-tagger
└── vitest.config.ts
```
